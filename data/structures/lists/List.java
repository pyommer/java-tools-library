package data.structures.lists;

/**
 * A generic list interface containing the methods used for storing and
 * manipulating objects in a list data structure. An object that implements
 * this List interface allows the objects in a generic array to be accessed
 * as a list of elements.
 *
 * @param <T>   The type of objects to be stored in the list.
 */
public interface List<T>
{
}
